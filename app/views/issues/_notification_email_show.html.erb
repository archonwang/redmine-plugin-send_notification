<%= issue_fields_rows do |rows|
  unless @issue.disabled_core_fields.include?('due_date')
      rows.right l(:field_recipient_email), @issue.recipient_email, :class => ':recipient_email'
  end
end %>
